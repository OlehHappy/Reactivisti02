# For React Unit testing

* `mocha` as test runner
* `babel-plugin-rewire`
* `sinon.js`
* `react-addon-test-util`

# Intl
* `Format.js` for localization from Yahoo! one of the best
* `react-intl` v2 from `Format.js` Yahoo!